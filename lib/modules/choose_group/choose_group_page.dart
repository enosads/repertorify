import 'package:flutter/material.dart';

class ChooseGroupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(
        child: Text('Choose a group page'),
      ),
    );
  }
}
